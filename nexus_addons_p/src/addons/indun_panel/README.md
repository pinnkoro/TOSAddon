# Indun Panel

> チャレンジやレイドに入る支援パネルを表示します

| 項目 | 内容 |
| --- | --- |
| キー | `indun_panel` |
| ソース | [indun_panel.lua](indun_panel.lua) |
| 設定画面 | あり（パネルの歯車ボタン。**画面中央にポップアップで開きます**） |

## 何をするアドオンか

**その日に回るコンテンツへの入場を 1 枚のパネルにまとめた**アドオンです。
レイド・チャレンジ・分裂特異点・ボス協同戦などの入場ボタンに加えて、
各種ショップやマーケットへのショートカット、入場券の所持数、
チャレンジマップの日替わりスケジュールまで表示します。

## 使い方

ON にして街に入ると `INDUNPANEL` ボタンが出ます。

| 操作 | 動作 |
| --- | --- |
| **左クリック** | パネルを展開／折りたたむ |
| **右クリック** | **常時展開**にして開く（展開したまま固定） |
| ドラッグ | 移動（位置は保存されます） |

展開すると、選択中のセットで表示 ON にしたコンテンツが並びます。
各行にはアイコン・名前・入場回数・**入場券の所持数**が出て、
クリックでそのコンテンツへ入場できます。

![展開した Indun Panel。上段は左から INDUNPANEL ボタン・共通ボタン・ショートカット・SET A / SET B / SET C の切り替え・常時展開のチェック・設定の歯車の順に並び、その下にコンテンツごとの行が SOLO・AUTO・HARD・ACLEAR・USE のボタンと回数付きで並ぶ。チャレンジと分裂特異点の行は 520 / 540 / 560 の段が横に並ぶ](images/01-panel.png)

### 共通ボタン

パネルの左側には常に次のボタンが並びます。

| ボタン | 左クリック | 右クリック |
| --- | --- | --- |
| バラックアイコン | **Indun List Viewer** を開く（無効なら Instant CC / 通常のバラック移動） | **Other Character Skill List** を開く |

展開すると、右端に次の 2 つが付きます。

| ボタン | 左クリック |
| --- | --- |
| チェックボックス | 常時展開の ON/OFF |
| 歯車 | 設定画面を開く |

歯車を**一番右**に置いているのは、畳んだ状態では歯車が出ないためです。共通ボタンと
ショートカットの間に挟むと、展開したときだけショートカットが 30px 右へずれます。

### ショートカットボタン

設定でチェックしたものが並びます。

| アイコン | 行き先 |
| --- | --- |
| `tos` | TOS イベントショップ |
| `gabija` / `vakarine` / `rada` / `jurate` / `austeja` / `saule` | 各女神ショップ（所持コイン数がツールチップに出ます）。サウレだけは**素にショップボタンの画像が無い**ため、コインの画像を使っています（素の `minimized_certificate_shop_button` はアウステヤの絵のままサウレの商店を開きます） |
| `pvp_mine` | 傭兵団ショップ |
| `market` | マーケット |
| `craft` | 装備加工 |
| `leticia` | レティーシャへ移動 |

### 表示セット（SET A / B / C）

**表示するコンテンツの組み合わせを 3 セット**持てます。
パネル上のセット名ボタンで切り替え、設定画面ではセット名を右クリックして改名できます。

「レイドの日」「チャレンジの日」など、曜日ごとに回るものを分けておく使い方を想定しています。

![SET A に切り替えたところ。選択中のセット名だけが赤くなり、そのセットで表示 ON にしたコンテンツだけが並ぶ](images/02-set.png)

## 対応コンテンツ

| キー | 表示名 |
| --- | --- |
| `challenge` | チャレンジ（ソロ 520 / ソロ 540 / ソロ 560 + PT 560） |
| `singularity` | 分裂特異点（520 / 540 / 560） |
| `light_uriel` | 偽りの輝翼（ハードは未実装） |
| `dark_uriel` | 堕落した審判の翼（ハードは未実装） |
| `zmei` | ズメイ |
| `belliora` | ベリオラ |
| `laimara` | ライマラ |
| `ledania` | レダニア |
| `neringa` | ネリンガ |
| `golem` | ゴーレム |
| `merregina` | メレジナ |
| `slogutis` | スローガティス |
| `upinis` | ウピニス |
| `roze` | ロゼ |
| `falouros` | ファロウロス |
| `reservoir` | プロパゲーター |
| `jellyzele` | ジェリージェル |
| `delmore` | デルムーア |
| `giltine` | ギルティネ |
| `memory` | 焔の記憶 |
| `telharsha` | テルハルシャ |
| `bernice` | ヴェルニケ |
| `wailing` | 嘆きの墓地 |
| `zawra` | 共鳴の聖所（ザウラ） |
| `jsr` | ボス協同戦 |

レイドは **ハード / ソロ / 自動** の 3 通りの入場に対応し、
掃討バフを持っている場合はその回数も表示します。
**ハードがまだ実装されていないレイド**（偽りの輝翼 / 堕落した審判の翼）では
HARD ボタンを出しません。実装されたら出るようにしてあります。

チャレンジと分裂特異点は **Lv 帯（段）ごとに横並び**です。Lv 上限が上がって段が増えると
そのぶんパネルが横に広がるので、**設定画面の「表示する段(Lv)」で使わない段だけを外せます**
（その行の段を全部外すと、行ごと消えます）。
Lv560 の入場券は TOS イベントショップと傭兵団ショップの両方で買えます
（傭兵団ショップの売り物は Lv560 用に差し替わっており、Lv540 用はもう買えません）。

入場券で入るパーティダンジョン（嘆きの墓地 / 共鳴の聖所）は、
Lv ボタンで入場ウィンドウを開き、`USE` ボタンで入場券を使います。
**共鳴の聖所だけは、ウィンドウを開くところで止めます**（入場は自分で押してください）。
嘆きの墓地はこれまでどおり、押すとウィンドウを開いた 0.3 秒後に入場まで進みます。

`ボス協同戦` を表示にしていると、サーバー時刻に応じて
ゲーム側のフィールドボスタブを自動で切り替えます（12:05 より前なら前半、以降は後半）。

### チャレンジマップのスケジュール

チャレンジの行にあるアイコンをクリックすると、**日替わりのチャレンジマップ一覧**が出ます。
基準日（初めて自動チャレンジマップに入った日）からの経過日数で計算しています。

## 設定

パネルの歯車ボタンから開きます。**パネルとは別のウィンドウ**として画面中央に出るので、
パネルを画面の端に置いていても設定は読みやすい位置に出ます。ESC でも × と同じように
閉じられます。

設定を変えると**その場でパネルを描き直します**（パネルは設定を開いている間も裏に出たまま
なので、変更がすぐ反映されます）。閉じても**展開／折りたたみの状態は変わりません**。

タブを切り替えても、セットを切り替えても**ウィンドウの位置はそのまま**です
（中身だけを入れ替えます）。

中身は **2 つのタブ**に分かれています。Nexus Addons P 本体の設定画面と同じ作りです。

| タブ | 中身 |
| --- | --- |
| **パネル** | `BASE POS` / `背景`（3 つのボタン）/ `表示・操作`（4 つのチェック）と、**パネルに並べるショートカット** |
| **コンテンツ** | **セットの切り替え（SET A / B / C）**と、レイド・コンテンツの表示 ON/OFF（＋チャレンジ / 分裂特異点の段） |

![Indun Panel 設定のパネルタブ。上に「パネル」「コンテンツ」のタブが並び、「Indun Panel の設定」の下に BASE POS ボタン、「背景」の見出しと いつもの / 黒 / 透明度高め の 3 ボタン（選択中の「透明度高め」だけ赤い）、「表示・操作」の見出しと 英語で表示 / フィールドでも表示 / フレームを固定 / 網掛けで表示 の 4 つのチェックが 2 列に並ぶ。その下の「パネルに並べるショートカット」では「ON のものだけ表示」ボタンと 11/11 件の件数が出て、スクロールする枠の中に ▲▼・チェック・アイコン・名前の行が TOS イベントショップから並び、街でだけ出るものには (都市) が付く](images/03-config-panel.png)

![Indun Panel 設定のコンテンツタブ。一覧の上に SET A / SET B / SET C のボタンが並び（選択中の SET A だけ赤い）、その下に「下のチェックはセットごとに保存 (右クリックで改名)」の注記と「レイド・コンテンツの表示 ON/OFF」の見出し、「ON のものだけ表示」ボタンと 25/25 件の件数が出る。スクロールする枠の中は 1 行 1 コンテンツで、▲▼・表示のチェック・コンテンツ名・パネルに出るのと同じ絵が並び、チャレンジと分裂特異点の行にはさらに 520 / 540 / 560 の段のチェックが付く](images/04-config-contents.png)

コンテンツの一覧は **1 行 1 コンテンツの縦 1 列**で、枠の中を**縦スクロール**します。
各行には**パネルに出るのと同じ絵**が名前の右に付くので、パネルのどの行のことなのかを
目で追い直さずに済みます。ショートカットの一覧も同じく枠の中でスクロールします。

**窓の大きさは両タブとも同じ（500 × 640）**で、中身の量でも変わりません。一覧は数が
増えるたびに伸びるので、窓の背丈で受け止めると画面に収まらなくなるためです
（1280x720 でも収まる大きさにしてあります）。タブごとに大きさを変えると、窓を画面の端へ
寄せているときにはみ出したぶんだけ戻され、元の位置には復帰しません（切り替えるたび
少しずつ寄っていく）。

セット名ボタン（SET A / B / C）はコンテンツタブの**一覧のすぐ上**に置いています。
セットが選ぶのは下に並ぶチェックの組み合わせなので、効く範囲の直前に置いて対応が分かる
ようにしています。

### 並べ替え（▲▼）

**ショートカットもコンテンツの行も、▲▼ でパネルに並ぶ順番を変えられます。**
よく使うものを手前に寄せておくと、パネルの左側だけ見れば済むようになります。

* 押すと**その場でパネルも並び直します**（設定を閉じるまで待つ必要はありません）。
* 順番は**セットとは無関係の共通設定**です。セットごとに順番まで変わると、切り替えるたびに
  並びが動いて位置を覚えられなくなるためです（段の設定と同じ持ち方）。
* **並べ替えた後に新しいダンジョンやショートカットが増えたときは、末尾に付きます。**
  覚えた並びの真ん中に知らない行が割り込まないようにしてあります。
* 表示を OFF にしている行も順番は覚えています。ON に戻すとその位置へ入ります。

### ON のものだけ表示

ショートカットとコンテンツのどちらにも **`ON のものだけ表示`** ボタンがあります。
チェックを入れているものだけに絞るので、**並べ替えたいものだけを並べて上下できます**
（25 行から探す必要がなくなります）。右隣に `12 / 25 件` のように件数が出ます。

* **絞っている間の▲▼は、隠れている行を飛ばして「見えている次の行」と入れ替えます。**
  隠れている行と素直に入れ替えると、画面上は何も動かないので「押しても効かない」ように
  見えるためです。飛ばされた行はその場に留まります。
* この絞り込みは**保存しません**。画面の見せ方だけなので、起動のたび「すべて表示」から
  始まります。
* **絞っている間にチェックを外すと、その行はその場で一覧から消えます**（件数も減ります）。
  戻したいときは `すべて表示` に切り替えてください。

| 項目 | 既定値 | 説明 |
| --- | --- | --- |
| `BASE POS` | — | パネルを既定位置（`665, 30`）に戻す |
| 背景 | いつもの | `背景` の見出しの下に `いつもの` / `黒` / `透明度高め` を横に並べたボタン。**今使っているものが赤くなります** |
| ショートカットのチェック（11 個） | すべて ON | ショートカットボタンを出すかどうか。**アイコンの右に行き先の名前が出ます**（`(都市)` と付いているものは街でだけ出ます） |
| ショートカットの▲▼ | 定義順 | パネルに並べる順番 |
| `ON のものだけ表示` | すべて表示 | 一覧をチェック済みのものだけに絞る（保存しない） |
| セット名ボタン | SET A/B/C | 左クリックで編集対象を切替、**右クリックで名前を変更** |
| コンテンツの行の▲▼ | 定義順 | パネルに並べる順番。**セット共通** |
| 英語で表示 | OFF | コンテンツ名を英語（内部キー）で表示する |
| フレームを固定 | OFF | ドラッグ移動を禁止する |
| フィールドでも表示 | OFF | **街以外でもパネルを表示する** |
| 網掛けで表示 | OFF | 行を 1 行おきに網掛けする |
| 段のチェック（チャレンジ / 分裂特異点の行の右にある 520 / 540 / 560） | すべて ON | **その段のボタンを出すかどうか**。外した段は描かれないぶん、行が短くなります。**その行の段を全部外すと行ごと消えます**（見出しだけの空の行が残らないようにするため）。**その行そのものを非表示にしていると数字が灰色**になります（段を触っても何も出ないため）。段の設定はセットとは無関係で、SET A/B/C 共通です |
| コンテンツごとのチェック | すべて ON | 選択中のセットで表示するかどうか |

## 保存先

`../addons/_nexus_addons_p/<アカウントID>/indun_panel.json`

```json
{
  "etc": { "challenge_ticket": "month", "always_open": 0, "singularity_check": 0,
           "skin_name": "chat_window_2", "en_ver": 0, "x": 665, "y": 30, "move": 0,
           "use_set": "set_a", "challenge_map": 0, "base_date": "",
           "shading": 0, "field_mode": 0, "toscoin": 0 },
  "cols": { "tos": 1, "gabija": 1, ... },
  "col_order": { "tos": 1, "gabija": 2, ... }, "row_order": { "challenge": 1, ... },
  "set_names": [ { "set_a": "SET A" }, { "set_b": "SET B" }, { "set_c": "SET C" } ],
  "set_a": { "challenge": 1, "zmei": 1, ... }, "set_b": {...}, "set_c": {...},
  "tiers": { "challenge": { "lv520": 1, "lv540": 1, "lv560": 1 },
             "singularity": { "lv520": 1, "lv540": 1, "lv560": 1 } }
}
```

`tiers` のキーが `lv520` のように接頭辞付きなのは、**`"520"` のような数字だけの文字列を
JSON の object キーにすると、実装によっては配列と取り違えられる**ためです。

`col_order` / `row_order` は▲▼で並べ替えたときだけ書かれます（**キー → 何番目か**）。
番号を持たないキーは末尾へ回るので、並べ替えた後に足されたものは自動で末尾に付きます。

新しいダンジョンが追加されたときは、読み込み時に**既存の設定へ「表示 ON」で補完**します。
Lv 上限が上がって段が増えたときも同じで、増えた段は既定 ON で補われます。
初回のみ、旧 `indun_panel` アドオンの
`../addons/indun_panel/<アカウントID>/settings.json` から引き継ぎます。

## 注意

* 既定では**街（`City`）でしか表示されません。**フィールドでも出したい場合は
  「フィールドでも表示」を ON にしてください。
  インスタンスマップとヴェルニケ（`8022`）では常に非表示です。
* 入場回数の記録は **Indun List Viewer** と共有しています（そちらが OFF でも記録は行われます）。
* 派生版での主な修正:
  * 新レイド「ズメイ」対応（v1.0.0）
  * ログイン時に採掘場ショップが一瞬開いてインベントリが閉じる不具合を解消。
    PVP_MINE の購入可能数の同期を**パネル展開時の遅延同期**（セッション中 1 回）へ変更（v1.0.0）
  * **ドラッグで移動しても位置が初期位置に戻る不具合**を修正（v1.0.0）
  * 設定ウィンドウを**タブ（パネル / コンテンツ）に分け**、ショートカットを
    **アイコン＋名前の一覧**にしました。右端で見切れて「レティーシャへ移動」を
    押せなかったのが直っています（次回リリース）
  * **ショートカットとコンテンツの行を▲▼で並べ替えられる**ようにしました（次回リリース）
  * 設定ウィンドウの**大きさを両タブで揃え（500 × 640）、ショートカットの一覧も
    スクロールする枠に入れました**。中身なりに縦へ伸ばしていると、ショートカットを全部
    出したとき 1280x720 の画面では最後の行が画面外に落ちて押せませんでした（次回リリース）
  * 背景の選択を `SKIN SELECT` ボタン + コンテキストメニューから、**3 つのボタンを横に
    並べて今使っているものを赤くする**形に変えました。あわせて `背景` `表示・操作` の
    見出しを足し、チェックの文字から「チェックすると」を落として名詞止めで揃えました
    （補足はツールチップに出ます）（次回リリース）
  * **アシャークを一覧から外しました**（入場できなくなったため）（次回リリース）
  * コンテンツの一覧を**縦 1 列 + スクロール**にし、**名前の右にパネルと同じ絵**を
    付けました。ショートカットとコンテンツのどちらにも**`ON のものだけ表示`**を
    足しています。絞っている間にチェックを外すとその行はその場で消えます（次回リリース）
  * 展開したパネルの**右端で歯車（設定を開くボタン）が切れていた**のを修正（パネルの幅を
    行の長さだけで決めていたため、ショートカットを 11 個出すと上段がはみ出していた）（次回リリース）
  * 設定ウィンドウで**セットを切り替えるたびに窓が画面中央へ戻っていた**のを修正（次回リリース）
